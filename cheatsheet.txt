This is the list of useful commands for Computer Science:

First Command: 
git clone --config="http.proxy=http://isaiah1782:christinawood@192.168.2.1:8080" https://github.com/isaiah1782-cmis/isaiah1782-cmis-wd.git

Other useful commands:

(Press Tab to complete words for files etc.)
ls
ls isaiah1782-cmis-wd/
cd
cd isaiah1782-cmis-wd/
cd ~
cd ..
gedit (filename) &
gedit (make up filename) & 
git add (filename)
git commit -m "(Commit message)"
git push

public_html:
Website: http://cmis-hsteacher/~(Username)
How to get to a file: http://cmis-hsteacher/~(Username)/L+L
Get to other folder/file: http://cmis-hsgit clone --config="http.proxy=http://isaiah1782:christinawood@192.168.2.1:8080" https://github.com/isaiah1782-cmis/isaiah1782-cmis-wd.gitteacher/~(Username)/L+L/other.html

user-cmis-wd:
-Open python -m SimpleHTTPServer
-go to localhost:8000

Make a directery/folder:
mkdir <foldername>

Remove a directery/folder (Has to be empty):
rmdir <foldername>

OVERALL COMMANDS:

Terminal Commands:

ls <directory name>:  List contents of a directory
cd: Go home.
cd <directory name>:  Change directory
cd ..: Go up
cat <file name>: View the contents of a file
gedit <file name> &
python -m SimpleHTTPServer - Start the development server
* - Select all

Git Commands:

git clone --config="http.proxy=http://uname:pword@192.168.2.1:8080" http://url-to-your-repo
git add <file name>
git commit -m "Commit Comment"
git push

sftp Commands:

sftp username@cmis-hsteacher
cd public_html/
ls - same as terminal
cd - same as terminal
prepend a command with ! to execute it on local
put <filename> - upload a file
get <filename> - download a file
exit - exits sftp
! - exits sftp
